{
  "name": "edge-backup-system",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:update": "playwright test --update-snapshots",
    "serve": "concurrently -n backend,frontend \"cd backend && python3 -m uvicorn main:app --port 8000 --host 127.0.0.1\" \"cd frontend && npm run dev -- --host 127.0.0.1\""
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "concurrently": "^9.2.1"
  }
}
